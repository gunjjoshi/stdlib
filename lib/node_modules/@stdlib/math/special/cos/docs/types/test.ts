/*
* @license Apache-2.0
*
* Copyright (c) 2025 The Stdlib Authors.
*
* Licensed under the Apache License, Version 2.0 (the "License");
* you may not use this file except in compliance with the License.
* You may obtain a copy of the License at
*
*    http://www.apache.org/licenses/LICENSE-2.0
*
* Unless required by applicable law or agreed to in writing, software
* distributed under the License is distributed on an "AS IS" BASIS,
* WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
* See the License for the specific language governing permissions and
* limitations under the License.
*/

/* eslint-disable space-in-parens */

import zeros = require( '@stdlib/ndarray/zeros' );
import cos = require( './index' );


// TESTS //

// The function returns an ndarray...
{
	const x = zeros( [ 2, 2 ], {
		'dtype': 'float64'
	});

	cos( x ); // $ExpectType typedndarray<number>
	cos( x, {} ); // $ExpectType typedndarray<number>
	cos( x, { 'dtype': 'float32' } ); // $ExpectType typedndarray<number>
	cos( x, { 'order': 'row-major' } ); // $ExpectType typedndarray<number>
}

// The compiler throws an error if the function is provided a first argument which is not an ndarray...
{
	cos( '5' ); // $ExpectError
	cos( 5 ); // $ExpectError
	cos( true ); // $ExpectError
	cos( false ); // $ExpectError
	cos( null ); // $ExpectError
	cos( void 0 ); // $ExpectError
	cos( [] ); // $ExpectError
	cos( {} ); // $ExpectError
	cos( [ '5' ] ); // $ExpectError
	cos( ( x: number ): number => x ); // $ExpectError
}

// The compiler throws an error if the function is provided a second argument which is not a valid options object...
{
	const x = zeros( [ 2, 2 ], {
		'dtype': 'float64'
	});

	cos( x, '5' ); // $ExpectError
	cos( x, true ); // $ExpectError
	cos( x, false ); // $ExpectError
	cos( x, [ '5' ] ); // $ExpectError
	cos( x, ( x: number ): number => x ); // $ExpectError
}

// The compiler throws an error if the function is provided an invalid `dtype` option...
{
	const x = zeros( [ 2, 2 ], {
		'dtype': 'float64'
	});

	cos( x, { 'dtype': '5' } ); // $ExpectError
	cos( x, { 'dtype': true } ); // $ExpectError
	cos( x, { 'dtype': false } ); // $ExpectError
	cos( x, { 'dtype': null } ); // $ExpectError
	cos( x, { 'dtype': {} } ); // $ExpectError
	cos( x, { 'dtype': [ '5' ] } ); // $ExpectError
	cos( x, { 'dtype': ( x: number ): number => x } ); // $ExpectError
}

// The compiler throws an error if the function is provided an invalid `order` option...
{
	const x = zeros( [ 2, 2 ], {
		'dtype': 'float64'
	});

	cos( x, { 'order': '5' } ); // $ExpectError
	cos( x, { 'order': true } ); // $ExpectError
	cos( x, { 'order': false } ); // $ExpectError
	cos( x, { 'order': null } ); // $ExpectError
	cos( x, { 'order': {} } ); // $ExpectError
	cos( x, { 'order': [ '5' ] } ); // $ExpectError
	cos( x, { 'order': ( x: number ): number => x } ); // $ExpectError
}

// The compiler throws an error if the function is provided an unsupported number of arguments...
{
	const x = zeros( [ 2, 2 ], {
		'dtype': 'float64'
	});

	cos(); // $ExpectError
	cos( x, {}, {} ); // $ExpectError
}

// Attached to the main function is an `assign` method which returns an ndarray...
{
	const x = zeros( [ 2, 2 ], {
		'dtype': 'float64'
	});

	cos.assign( x, x ); // $ExpectType float64ndarray
}

// The compiler throws an error if the `assign` method is provided a first argument which is not an ndarray...
{
	const x = zeros( [ 2, 2 ], {
		'dtype': 'float64'
	});

	cos.assign( '5', x ); // $ExpectError
	cos.assign( true, x ); // $ExpectError
	cos.assign( false, x ); // $ExpectError
	cos.assign( null, x ); // $ExpectError
	cos.assign( void 0, x ); // $ExpectError
	cos.assign( {}, x ); // $ExpectError
	cos.assign( ( x: number ): number => x, x ); // $ExpectError
}

// The compiler throws an error if the `assign` method is provided a second argument which is not an ndarray...
{
	const x = zeros( [ 2, 2 ], {
		'dtype': 'float64'
	});

	cos.assign( x, '5' ); // $ExpectError
	cos.assign( x, true ); // $ExpectError
	cos.assign( x, false ); // $ExpectError
	cos.assign( x, null ); // $ExpectError
	cos.assign( x, void 0 ); // $ExpectError
	cos.assign( x, {} ); // $ExpectError
	cos.assign( x, ( x: number ): number => x ); // $ExpectError
}

// The compiler throws an error if the `assign` method is provided an unsupported number of arguments...
{
	const x = zeros( [ 2, 2 ], {
		'dtype': 'float64'
	});

	cos.assign(); // $ExpectError
	cos.assign( x ); // $ExpectError
	cos.assign( x, x, x ); // $ExpectError
}
