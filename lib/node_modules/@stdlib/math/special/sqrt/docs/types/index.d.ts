/*
* @license Apache-2.0
*
* Copyright (c) 2025 The Stdlib Authors.
*
* Licensed under the Apache License, Version 2.0 (the "License");
* you may not use this file except in compliance with the License.
* You may obtain a copy of the License at
*
*    http://www.apache.org/licenses/LICENSE-2.0
*
* Unless required by applicable law or agreed to in writing, software
* distributed under the License is distributed on an "AS IS" BASIS,
* WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
* See the License for the specific language governing permissions and
* limitations under the License.
*/

// TypeScript Version: 4.1

/// <reference types="@stdlib/types"/>

import { typedndarray, realndarray, floatndarray, genericndarray, RealAndGenericDataType as DataType, Order } from '@stdlib/types/ndarray';

/**
* Input array.
*/
type InputArray = realndarray | genericndarray<number>;

/**
* Output array.
*/
type OutputArray = floatndarray | genericndarray<number>;

/**
* Interface describing options.
*/
interface Options {
	/**
	* Output array order.
	*
	* ## Notes
	*
	* -   By default, the order of the output array is the same as the input array.
	*/
	order?: Order;

	/**
	* Output array data type.
	*/
	dtype?: DataType;
}

/**
* Interface describing a unary element-wise function.
*/
interface UnaryFunction {
	/**
	* Computes the principal square root for each element in an ndarray.
	*
	* @param x - input ndarray
	* @param options - options
	* @returns output ndarray
	*
	* @example
	* var ndarray2array = require( '@stdlib/ndarray/to-array' );
	* var array = require( '@stdlib/ndarray/array' );
	*
	* var x = array( [ [ 0, 0.01 ], [ 0.25, 0.5 ] ] );
	* // returns <ndarray>
	*
	* var y = sqrt( x );
	* // returns <ndarray>
	*
	* var arr = ndarray2array( y );
	* // returns [ [ ~0, ~0.1 ], [ ~0.5, ~0.71 ] ]
	*/
	( x: InputArray, options?: Options ): typedndarray<number>; // FIXME: we lose type specificity here, as the output ndarray data type is determined according to the output data type policy in conjunction with the `dtype` option

	/**
	* Compute the principal square root for each element in an ndarray. for each element in an ndarray and assigns results to a provided output ndarray.
	*
	* @param x - input ndarray
	* @param y - output ndarray
	* @returns output ndarray
	*
	* @example
	* var ndarray2array = require( '@stdlib/ndarray/to-array' );
	* var array = require( '@stdlib/ndarray/array' );
	*
	* var x = array( [ [ 0, 0.01 ], [ 0.25, 0.5 ] ] );
	* var y = array( [ [ 0.0, 0.0 ], [ 0.0, 0.0 ] ] );
	*
	* var z = sqrt.assign( x, y );
	* // returns <ndarray>
	*
	* var bool = ( z === y );
	* // returns true
	*
	* var arr = ndarray2array( y );
	* // returns [ [ ~0, ~0.1 ], [ ~0.5, ~0.71 ] ]
	*/
	assign<T extends OutputArray = OutputArray>( x: InputArray, y: T ): T;
}

/**
* Computes the principal square root for each element in an ndarray.
*
* @param x - input ndarray
* @param options - options
* @returns output ndarray
*
* @example
* var ndarray2array = require( '@stdlib/ndarray/to-array' );
* var array = require( '@stdlib/ndarray/array' );
*
* var x = array( [ [ 0, 0.01 ], [ 0.25, 0.5 ] ] );
*
* var y = sqrt( x );
* // returns <ndarray>
*
* var arr = ndarray2array( y );
* // returns [ [ ~0, ~0.1 ], [ ~0.5, ~0.71 ] ]
*
* @example
* var ndarray2array = require( '@stdlib/ndarray/to-array' );
* var array = require( '@stdlib/ndarray/array' );
*
* var x = array( [ [ 0, 0.01 ], [ 0.25, 0.5 ] ] );
* var y = array( [ [ 0.0, 0.0 ], [ 0.0, 0.0 ] ] );
*
* var z = sqrt.assign( x, y );
* // returns <ndarray>
*
* var bool = ( z === y );
* // returns true
*
* var arr = ndarray2array( y );
* // returns [ [ ~0, ~0.1 ], [ ~0.5, ~0.71 ] ]
*/
declare var sqrt: UnaryFunction;


// EXPORTS //

export = sqrt;
