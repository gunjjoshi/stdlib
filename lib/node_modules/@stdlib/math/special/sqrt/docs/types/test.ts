/*
* @license Apache-2.0
*
* Copyright (c) 2025 The Stdlib Authors.
*
* Licensed under the Apache License, Version 2.0 (the "License");
* you may not use this file except in compliance with the License.
* You may obtain a copy of the License at
*
*    http://www.apache.org/licenses/LICENSE-2.0
*
* Unless required by applicable law or agreed to in writing, software
* distributed under the License is distributed on an "AS IS" BASIS,
* WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
* See the License for the specific language governing permissions and
* limitations under the License.
*/

/* eslint-disable space-in-parens */

import zeros = require( '@stdlib/ndarray/zeros' );
import sqrt = require( './index' );


// TESTS //

// The function returns an ndarray...
{
	const x = zeros( [ 2, 2 ], {
		'dtype': 'float64'
	});

	sqrt( x ); // $ExpectType typedndarray<number>
	sqrt( x, {} ); // $ExpectType typedndarray<number>
	sqrt( x, { 'dtype': 'float32' } ); // $ExpectType typedndarray<number>
	sqrt( x, { 'order': 'row-major' } ); // $ExpectType typedndarray<number>
}

// The compiler throws an error if the function is provided a first argument which is not an ndarray...
{
	sqrt( '5' ); // $ExpectError
	sqrt( 5 ); // $ExpectError
	sqrt( true ); // $ExpectError
	sqrt( false ); // $ExpectError
	sqrt( null ); // $ExpectError
	sqrt( void 0 ); // $ExpectError
	sqrt( [] ); // $ExpectError
	sqrt( {} ); // $ExpectError
	sqrt( [ '5' ] ); // $ExpectError
	sqrt( ( x: number ): number => x ); // $ExpectError
}

// The compiler throws an error if the function is provided a second argument which is not a valid options object...
{
	const x = zeros( [ 2, 2 ], {
		'dtype': 'float64'
	});

	sqrt( x, '5' ); // $ExpectError
	sqrt( x, true ); // $ExpectError
	sqrt( x, false ); // $ExpectError
	sqrt( x, [ '5' ] ); // $ExpectError
	sqrt( x, ( x: number ): number => x ); // $ExpectError
}

// The compiler throws an error if the function is provided an invalid `dtype` option...
{
	const x = zeros( [ 2, 2 ], {
		'dtype': 'float64'
	});

	sqrt( x, { 'dtype': '5' } ); // $ExpectError
	sqrt( x, { 'dtype': true } ); // $ExpectError
	sqrt( x, { 'dtype': false } ); // $ExpectError
	sqrt( x, { 'dtype': null } ); // $ExpectError
	sqrt( x, { 'dtype': {} } ); // $ExpectError
	sqrt( x, { 'dtype': [ '5' ] } ); // $ExpectError
	sqrt( x, { 'dtype': ( x: number ): number => x } ); // $ExpectError
}

// The compiler throws an error if the function is provided an invalid `order` option...
{
	const x = zeros( [ 2, 2 ], {
		'dtype': 'float64'
	});

	sqrt( x, { 'order': '5' } ); // $ExpectError
	sqrt( x, { 'order': true } ); // $ExpectError
	sqrt( x, { 'order': false } ); // $ExpectError
	sqrt( x, { 'order': null } ); // $ExpectError
	sqrt( x, { 'order': {} } ); // $ExpectError
	sqrt( x, { 'order': [ '5' ] } ); // $ExpectError
	sqrt( x, { 'order': ( x: number ): number => x } ); // $ExpectError
}

// The compiler throws an error if the function is provided an unsupported number of arguments...
{
	const x = zeros( [ 2, 2 ], {
		'dtype': 'float64'
	});

	sqrt(); // $ExpectError
	sqrt( x, {}, {} ); // $ExpectError
}

// Attached to the main function is an `assign` method which returns an ndarray...
{
	const x = zeros( [ 2, 2 ], {
		'dtype': 'float64'
	});

	sqrt.assign( x, x ); // $ExpectType float64ndarray
}

// The compiler throws an error if the `assign` method is provided a first argument which is not an ndarray...
{
	const x = zeros( [ 2, 2 ], {
		'dtype': 'float64'
	});

	sqrt.assign( '5', x ); // $ExpectError
	sqrt.assign( true, x ); // $ExpectError
	sqrt.assign( false, x ); // $ExpectError
	sqrt.assign( null, x ); // $ExpectError
	sqrt.assign( void 0, x ); // $ExpectError
	sqrt.assign( {}, x ); // $ExpectError
	sqrt.assign( ( x: number ): number => x, x ); // $ExpectError
}

// The compiler throws an error if the `assign` method is provided a second argument which is not an ndarray...
{
	const x = zeros( [ 2, 2 ], {
		'dtype': 'float64'
	});

	sqrt.assign( x, '5' ); // $ExpectError
	sqrt.assign( x, true ); // $ExpectError
	sqrt.assign( x, false ); // $ExpectError
	sqrt.assign( x, null ); // $ExpectError
	sqrt.assign( x, void 0 ); // $ExpectError
	sqrt.assign( x, {} ); // $ExpectError
	sqrt.assign( x, ( x: number ): number => x ); // $ExpectError
}

// The compiler throws an error if the `assign` method is provided an unsupported number of arguments...
{
	const x = zeros( [ 2, 2 ], {
		'dtype': 'float64'
	});

	sqrt.assign(); // $ExpectError
	sqrt.assign( x ); // $ExpectError
	sqrt.assign( x, x, x ); // $ExpectError
}
