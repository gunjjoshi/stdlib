/*
* @license Apache-2.0
*
* Copyright (c) 2025 The Stdlib Authors.
*
* Licensed under the Apache License, Version 2.0 (the "License");
* you may not use this file except in compliance with the License.
* You may obtain a copy of the License at
*
*    http://www.apache.org/licenses/LICENSE-2.0
*
* Unless required by applicable law or agreed to in writing, software
* distributed under the License is distributed on an "AS IS" BASIS,
* WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
* See the License for the specific language governing permissions and
* limitations under the License.
*/

/* eslint-disable space-in-parens */

import zeros = require( '@stdlib/ndarray/zeros' );
import erf = require( './index' );


// TESTS //

// The function returns an ndarray...
{
	const x = zeros( [ 2, 2 ], {
		'dtype': 'float64'
	});

	erf( x ); // $ExpectType typedndarray<number>
	erf( x, {} ); // $ExpectType typedndarray<number>
	erf( x, { 'dtype': 'float32' } ); // $ExpectType typedndarray<number>
	erf( x, { 'order': 'row-major' } ); // $ExpectType typedndarray<number>
}

// The compiler throws an error if the function is provided a first argument which is not an ndarray...
{
	erf( '5' ); // $ExpectError
	erf( 5 ); // $ExpectError
	erf( true ); // $ExpectError
	erf( false ); // $ExpectError
	erf( null ); // $ExpectError
	erf( void 0 ); // $ExpectError
	erf( [] ); // $ExpectError
	erf( {} ); // $ExpectError
	erf( [ '5' ] ); // $ExpectError
	erf( ( x: number ): number => x ); // $ExpectError
}

// The compiler throws an error if the function is provided a second argument which is not a valid options object...
{
	const x = zeros( [ 2, 2 ], {
		'dtype': 'float64'
	});

	erf( x, '5' ); // $ExpectError
	erf( x, true ); // $ExpectError
	erf( x, false ); // $ExpectError
	erf( x, [ '5' ] ); // $ExpectError
	erf( x, ( x: number ): number => x ); // $ExpectError
}

// The compiler throws an error if the function is provided an invalid `dtype` option...
{
	const x = zeros( [ 2, 2 ], {
		'dtype': 'float64'
	});

	erf( x, { 'dtype': '5' } ); // $ExpectError
	erf( x, { 'dtype': true } ); // $ExpectError
	erf( x, { 'dtype': false } ); // $ExpectError
	erf( x, { 'dtype': null } ); // $ExpectError
	erf( x, { 'dtype': {} } ); // $ExpectError
	erf( x, { 'dtype': [ '5' ] } ); // $ExpectError
	erf( x, { 'dtype': ( x: number ): number => x } ); // $ExpectError
}

// The compiler throws an error if the function is provided an invalid `order` option...
{
	const x = zeros( [ 2, 2 ], {
		'dtype': 'float64'
	});

	erf( x, { 'order': '5' } ); // $ExpectError
	erf( x, { 'order': true } ); // $ExpectError
	erf( x, { 'order': false } ); // $ExpectError
	erf( x, { 'order': null } ); // $ExpectError
	erf( x, { 'order': {} } ); // $ExpectError
	erf( x, { 'order': [ '5' ] } ); // $ExpectError
	erf( x, { 'order': ( x: number ): number => x } ); // $ExpectError
}

// The compiler throws an error if the function is provided an unsupported number of arguments...
{
	const x = zeros( [ 2, 2 ], {
		'dtype': 'float64'
	});

	erf(); // $ExpectError
	erf( x, {}, {} ); // $ExpectError
}

// Attached to the main function is an `assign` method which returns an ndarray...
{
	const x = zeros( [ 2, 2 ], {
		'dtype': 'float64'
	});

	erf.assign( x, x ); // $ExpectType float64ndarray
}

// The compiler throws an error if the `assign` method is provided a first argument which is not an ndarray...
{
	const x = zeros( [ 2, 2 ], {
		'dtype': 'float64'
	});

	erf.assign( '5', x ); // $ExpectError
	erf.assign( true, x ); // $ExpectError
	erf.assign( false, x ); // $ExpectError
	erf.assign( null, x ); // $ExpectError
	erf.assign( void 0, x ); // $ExpectError
	erf.assign( {}, x ); // $ExpectError
	erf.assign( ( x: number ): number => x, x ); // $ExpectError
}

// The compiler throws an error if the `assign` method is provided a second argument which is not an ndarray...
{
	const x = zeros( [ 2, 2 ], {
		'dtype': 'float64'
	});

	erf.assign( x, '5' ); // $ExpectError
	erf.assign( x, true ); // $ExpectError
	erf.assign( x, false ); // $ExpectError
	erf.assign( x, null ); // $ExpectError
	erf.assign( x, void 0 ); // $ExpectError
	erf.assign( x, {} ); // $ExpectError
	erf.assign( x, ( x: number ): number => x ); // $ExpectError
}

// The compiler throws an error if the `assign` method is provided an unsupported number of arguments...
{
	const x = zeros( [ 2, 2 ], {
		'dtype': 'float64'
	});

	erf.assign(); // $ExpectError
	erf.assign( x ); // $ExpectError
	erf.assign( x, x, x ); // $ExpectError
}
